import {button} from "../../../../assets/styles/jss/globalStyles";

export const supplierStyles = (theme) => ({
    submitButton: {
        ...button,
        margin: "1rem 0"
    },
    listContainer: {
        height: "60vh",
        padding: "1rem",
        minWidth: "60rem",
        overflow: "auto"
    },
    button,
});