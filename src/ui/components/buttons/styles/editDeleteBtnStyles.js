import {blue, red} from "@material-ui/core/colors";

export const editDeleteBtnStyles = (theme) => ({
    content: {
        textAlign: "right"
    },
    iconEdit: {
        color: blue[500]
    },
    iconDelete: {
        color: red[500]
    }
});
